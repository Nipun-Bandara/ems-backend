spring:
  config:
    import: optional:file:./.env.staging[.properties],optional:file:./api-gateway/.env.staging[.properties]
  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: identity-service
              uri: ${IDENTITY_SERVICE_URL}
              predicates:
                - Path=/api/auth/**, /api/users/**, /api/departments/**

app:
  security:
    jwt:
      secret: ${JWT_SECRET}